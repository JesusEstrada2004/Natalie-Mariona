---
const { post } = Astro.props;
---

<article
  class="bg-white rounded-2xl shadow-md overflow-hidden hover:shadow-lg transition"
>
  <a href={`/blog/${post.slug}`} class="block">
    <img src={post.image} alt={post.title} class="w-full h-56 object-cover" />
  </a>

  <div class="p-6 flex flex-col justify-between h-full">
    <div>
      <h2 class="text-xl font-bold text-[#2F2623] mb-2">
        <a href={`/blog/${post.slug}`} class="hover:underline">
          {post.title}
        </a>
      </h2>
      <p class="text-[#5C4636] text-sm mb-4">{post.description}</p>
    </div>

    <a
      href={`/blog/${post.slug}`}
      class="text-[#2F2623] font-semibold hover:text-[#F9C74F] transition"
    >
      Mas →
    </a>
  </div>
</article>
